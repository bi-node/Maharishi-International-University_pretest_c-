#include<iostream>
#include<stdlib.h>
using namespace std;
int allPossibilities(int a[], int len)
{
    int flag=0;
    for (int i=0; i<len; i++)
    {
            
    }
}

int main()
{
    
    int a[]={1, 2, 0, 3};
    int len=sizeof(a)/sizeof(a[0]);
    cout<<allPossibilities(a,len);
    return 0;
}